"""The ways in: an HTTP API for the web app, A2A for peer agents, MCP for a
single model's tools. All three are thin — they translate a request into a call
on `knowli.application` and translate the result back."""
