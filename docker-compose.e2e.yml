services:
  api:
    environment:
      # The regular Compose file always uses the real OpenAI adapter. This override
      # is used exclusively by Playwright so CI never needs an AI credential.
      KNOWLI_E2E_DEPENDENCIES: "true"
      OPENAI_API_KEY: ""

volumes:
  knowli_pgdata:
    name: knowli_e2e_pgdata
