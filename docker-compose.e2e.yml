services:
  api:
    environment:
      # The regular Compose file uses the configured model adapter. This override
      # is used exclusively by Playwright so CI never needs an AI credential.
      KNOWLI_E2E_DEPENDENCIES: "true"

volumes:
  knowli_pgdata:
    name: knowli_e2e_pgdata
