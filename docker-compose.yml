services:
  db:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: ke
      POSTGRES_PASSWORD: ke
      POSTGRES_DB: ke
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ke"]
      interval: 3s
      timeout: 3s
      retries: 20

volumes:
  pgdata:
